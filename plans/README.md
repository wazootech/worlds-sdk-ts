# Implementation Plans

Generated by the improve skill on 2026-06-10 at commit `73a0920`. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Make LibSQL replace imports atomic with respect to wipe + rewrite | P1 | M | — | DONE |
| 002 | Serialize Deno KV batch writes and expose generation only after success | P1 | M | — | DONE |
| 003 | Make Transaction.commit fail fast when no persistence handler exists | P2 | S | — | TODO |
| 004 | Cap or hash oversized Deno KV key segments derived from RDF terms | P2 | M | — | TODO |
| 005 | Put a hard limit on search filter expansion before SQL generation | P2 | S/M | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- No plan requires another to land first.
- 001 and 002 are the durability-critical fixes for the two durable backends (LibSQL replace-wipe window; Deno KV generation flip + concurrent batch commits). Prioritize them before the smaller guards.
- 002 and 004 both touch `src/denokv/kv/` — execute sequentially (002 first) to avoid merge conflicts, even though neither logically depends on the other.

## Findings considered and rejected

- **DX-01 "Replace the broken `deno ci` workflow step"**: false positive. `deno ci` is a valid Deno 2.8 subcommand (lockfile-strict dependency install, analogous to `npm ci`); the workflows correctly run it as the install step before `deno task ci`. Recent GitHub Actions runs on main are green. Do not re-audit.
